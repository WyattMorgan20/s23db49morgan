extends layout

block content
  h1= title 

  div(class = "container-fluid")
    div(class = "row")
      - let result = results
        each item in result
          div(class = "col-sm-2") #{item.year} #{item.country} #{item.name}