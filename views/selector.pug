extends layout

block content
  h1= title
  - image_names = ['abramsx.jpg', 'challengerTank.jpg', 'M1A2Tank.jpg', 'pantherTank.jpeg', 'russianTank.jpg']

  - for(i = 0; i<3; i++){
      - var rand = Math.floor(Math.random()*5) 
      - var img = "/images/" + image_names[rand]
      img(src = img, width="300px", height="300px")
  -}